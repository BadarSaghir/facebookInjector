{
	"private": true,
	"scripts": {
		"build": "parcel build source/manifest.json --no-content-hash --no-source-maps --dist-dir facebookTweetInjector --no-cache --detailed-report 0",
		"lint": "run-p lint:*",
		"lint-fix": "run-p 'lint:* -- --fix'",
		"lint:css": "stylelint source/**/*.css",
		"lint:js": "xo",
		"test": "run-p lint:* build",
		"watch": "parcel watch source/manifest.json --no-content-hash --dist-dir facebookTweetInjector --no-cache --no-hmr"
	},
	"browserslist": [
		"last 1 Chrome version",
		"last 1 Firefox version"
	],
	"xo": {
		"envs": [
			"browser"
		],
		"rules": {
			"no-unused-vars": [
				"error",
				{
					"varsIgnorePattern": "browser"
				}
			]
		}
	},
	"stylelint": {
		"extends": "stylelint-config-xo"
	},
	"dependencies": {
		"@types/axios": "^0.14.0",
		"axios": "^0.27.2",
		"twitter-webscraper": "^1.0.2",
		"webext-base-css": "^1.4.0",
		"webext-options-sync": "^3.0.1"
	},
	"devDependencies": {
		"@parcel/config-webextension": "^2.5.0",
		"@types/chrome": "^0.0.193",
		"@types/webextension-polyfill": "^0.9.0",
		"buffer": "^6.0.3",
		"npm-run-all": "^4.1.5",
		"parcel": "^2.5.0",
		"process": "^0.11.10",
		"stylelint": "^14.6.0",
		"stylelint-config-xo": "^0.21.0",
		"xo": "^0.48.0"
	},
	"webExt": {
		"sourceDir": "facebookTweetInjector",
		"run": {
			"startUrl": [
				"https://www.google.com/"
			]
		}
	}
}
